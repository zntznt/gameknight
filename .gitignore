node_modules/
package-lock.json
.DS_Store
*.log
# Never commit a local token file. BGG_TOKEN should live in the environment
# (local shell) or a GitHub Actions secret, not on disk.
.env
.env.*

# playwright-core is installed ad hoc for browser verification only.
# It is not a project dependency; keep it out of package.json.

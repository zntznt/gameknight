{
  "$comment": "List the BGG usernames whose collections should be baked into data/games.json. The GitHub Action reads this file. 'id' is a short internal key, 'label' is what shows in the UI. Add more entries to pull a whole friend group.",
  "site": {
    "$comment": "CHANGE THESE WHEN YOU FORK. 'repoUrl' is where the header's GitHub badge points. 'appUrl' is where your copy is published, and it goes into the User-Agent the fetcher sends, which is how BGG matches API traffic to your registered application. Leaving mine here would report your requests as my app.",
    "repoUrl": "https://github.com/zntznt/gameknight",
    "appUrl": "https://zntznt.com/gameknight"
  },
  "collections": [
    { "id": "zntznt", "label": "Zeo", "bggUser": "zntznt" },
    { "id": "gabo", "label": "Gabo", "bggUser": "AranGba" },
    { "id": "diego", "label": "Diego", "bggUser": "Niv_Mizzet" }
  ],
  "options": {
    "$comment": "Which BGG statuses to pull per user. Leave own=true for a 'what can we play tonight' shelf. These combine with OR and the app can't tell them apart, so only enable wishlist/preordered if you truly want those games mixed in.",
    "own": true,
    "wishlist": false,
    "preordered": false
  }
}
